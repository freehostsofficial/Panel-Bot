# ============================================
# Discord Bot Configuration Template
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================
# Node Environment
# ============================================
# Options: development, production, staging
NODE_ENV=development

# ============================================
# Discord Bot Settings
# ============================================
# Get these from: https://discord.com/developers/applications

# Your bot token (REQUIRED)
# NEVER share this token publicly!
SETTINGS_BOT_TOKEN=your_bot_token_here

# Your application client ID (REQUIRED)
SETTINGS_BOT_CLIENTID=your_client_id_here

# Your application client secret (optional)
SETTINGS_BOT_CLIENTSECRET=your_client_secret_here

# ============================================
# Developer/Owner Configuration
# ============================================
# Comma-separated list of Discord user IDs with developer access
# Example: 123456789012345678,987654321098765432
SETTINGS_DEVELOPER_IDS=your_discord_user_id_here

# Comma-separated list of Discord user IDs with owner access
SETTINGS_DEVELOPER_OWNER_IDS=your_discord_user_id_here

# Comma-separated list of guild/server IDs for development/testing
# Leave empty for all servers
SETTINGS_DEVELOPER_GUILDIDS=

# ============================================
# Bot Status Configuration
# ============================================
# Status rotation interval in milliseconds (default: 30000 = 30 seconds)
SETTINGS_STATUSROTATIONINTERVALMS=30000

# ============================================
# Database Advanced Settings
# ============================================
# Enable database query debugging (shows all queries in console)
DB_DEBUG=false

# Disable automatic database initialization on startup
# Set to false if you want to initialize manually
DB_AUTO_INIT=true

# ============================================
# Environment Variable Naming Convention
# ============================================
# The config system automatically maps environment variables to JSON config files:
# 
# Format: {FILE}_{NESTED_PATH}
# Examples:
#   SETTINGS_BOT_TOKEN      → settings.json: { "bot": { "token": "..." } }
#   SERVER_POSTGRES_HOST    → server.json: { "postgres": { "host": "..." } }
#   LOGS_ERRORLOGS_WEBHOOK  → logs.json: { "errorLogs": { "webhook": "..." } }
#
# - Use underscores (_) or hyphens (-) to separate nested levels
# - Case-insensitive matching
# - Arrays can be specified with [] suffix or comma-separated values

# ============================================
# Example: Custom Settings Override
# ============================================
# You can override ANY setting from settings.json, server.json, logs.json, etc.
# using the FILENAME_NESTED_PATH format:

# SETTINGS_DEVELOPER_IDS=123456,789012
# LOGS_ERRORLOGS_ENABLED=false

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. NEVER commit the .env file to version control
# 2. NEVER share your bot token or database credentials
# 3. Use strong, unique passwords for database access
# 4. In production, ensure NODE_ENV=production
# 5. Enable SSL/TLS for database connections in production (SERVER_POSTGRES_SSL=true)
# 6. Restrict developer/owner IDs to trusted users only
# 7. Regularly rotate sensitive credentials
# 8. Use environment-specific .env files (.env.production, .env.development)
# 9. Keep this .env.example file updated when adding new configuration options

# ============================================
# Quick Start Minimal Configuration
# ============================================
# For a basic bot setup, you only need:
# 1. SETTINGS_BOT_TOKEN
# 2. SETTINGS_BOT_CLIENTID
# 3. SETTINGS_DEVELOPER_IDS
#
# Database is optional but recommended for persistence.
